<resources>

    <!-- Base application theme. -->
    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/popularmovies_grey_blue</item>
        <item name="colorPrimaryDark">@color/popularmovies_dark_grey_blue</item>
    </style>

</resources>
