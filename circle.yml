dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "android-23"
    - echo y | android update sdk --no-ui --all --filter "build-tools-23.0.1"
    - echo y | android update sdk --no-ui --all --filter "extra-android-support-23.0.1"
